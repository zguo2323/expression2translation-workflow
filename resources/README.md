# 外部资源

`reference/`：后续下载统一 release 的 genome、annotation、transcriptome 与 rRNA 资源；已被 Git 忽略。
`indices/`：后续构建的 Salmon、Bowtie2/STAR 索引；已被 Git 忽略。

阶段 2 已锁定并准备 Ensembl release 115 / R64-1-1，manifest 位于 `metadata/reference-ensembl-115.json`。大型序列仍被忽略。获取、派生和校验方法见 `docs/rnaseq.md`。
